# HR Endless Sampler 使用手册

`HR Endless Sampler` 是 MiniMax H3 的低显存长视频分块采样器。它把完整视频按时间分块依次生成，并使用本地多模态导演模型规划镜头、观察上一块画面，再为下一块编写 MiniMax H3 提示词。

支持的本地导演：

- **Gemma 4**：默认后端，兼容原有工作流，支持可选 MTP 和预制作 KV 缓存。
- **Qwen3.5**：适合中文输入，输出给 H3 的视觉、动作和镜头描述以英文为主，并保留原始中文对白。

导演、视频 VAE 和 H3 严格串行使用显存。导演运行在独立进程中，进程退出并释放 llama.cpp CUDA 资源后才重新加载 H3。

## 1. 安装

将插件放入：

```text
ComfyUI/custom_nodes/ComfyUI-MiniMax-H3-Sampler-Unlimited/
```

使用 ComfyUI 自己的 Python 安装依赖：

```bash
python -m pip install -r custom_nodes/ComfyUI-MiniMax-H3-Sampler-Unlimited/requirements.txt
```

插件当前固定使用：

```text
llama-cpp-python == 0.3.35
```

安装或更新后重启 ComfyUI。

## 2. 本地导演模型目录

### 2.1 Qwen3.5

推荐目录：

```text
ComfyUI/models/LLM/GGUF/qwen3.5-9B/
├── Huihui-Qwen3.5-9B-abliterated.Q4_K_M.gguf
└── mmproj-Huihui-Qwen3.5-9B-abliterated.gguf
```

插件会递归扫描：

```text
models/LLM/GGUF/
models/llama_cpp/
```

当 Qwen 模型和 mmproj 位于同一目录时，`auto` 会自动把它们配成一组。

Qwen不会联网下载模型，也不会使用 Transformers 或 Hugging Face 自动回退。

### 2.2 Gemma 4

默认目录：

```text
ComfyUI/models/llama_cpp/gemma-4-12b-it-qat-q4_0/
├── gemma-4-12b-it-qat-q4_0.gguf
├── mmproj-gemma-4-12b-it-qat-q4_0.gguf
└── gemma-4-12B-it-qat-assistant-MTP-Q8_0.gguf   # 仅MTP需要
```

旧工作流继续使用 Gemma 默认路径。也可以在节点中显式选择本地 Gemma model 和 mmproj。

> 只有官方默认 Gemma 模型和 projector 组合允许启用 `gemma4_mtp`，避免把不匹配的 MTP assistant 加载到其他模型。

## 3. 工作流连接

用 `HR Endless Sampler` 替换普通的 `SamplerCustomAdvanced`，保留原采样器的主要连接：

- `noise`
- `guider`
- `sampler`
- `sigmas`
- `latent_image`

额外连接：

| 输入 | 用途 |
| --- | --- |
| `clip` | MiniMax H3 使用的 CLIP/Qwen conditioning 模型 |
| `prompt` | 完整的原始 H3 提示词 |
| `fps` | 提示词时间码换算帧数，MiniMax H3 通常使用 24 |
| `vae` | H3 视频 VAE，用于续接和导演观察上一块画面 |
| `images` | 旧版批量参考图输入 |
| `source_images` | Ref2VA 多图输入，按 `source_image_0`、`source_image_1` 顺序连接 |

`images` 和 `source_images` 不要同时连接。

推荐把 `HR Endless Sampler Preview` 放在模型到 guider 的路径中，以查看累计预览、分块范围、镜头范围和每块导演提示词。

## 4. 导演设置

### 4.1 使用 Qwen3.5

```text
director_backend = qwen3.5
director_model = auto
director_mmproj = auto
cache_gemma_preproduction = false
gemma4_mtp = false
```

Qwen固定使用：

```text
context = 4096
batch = 256
MTP = 禁用
跨执行KV缓存 = 禁用
```

如果自动选择不正确，可在下拉框中明确选择：

```text
director_model = LLM/GGUF/qwen3.5-9B/Huihui-Qwen3.5-9B-abliterated.Q4_K_M.gguf
director_mmproj = LLM/GGUF/qwen3.5-9B/mmproj-Huihui-Qwen3.5-9B-abliterated.gguf
```

Qwen模式不支持Gemma的MTP和预制作KV缓存；启用这些选项会明确报错，不会静默降级。

### 4.2 使用 Gemma 4

兼容旧工作流的默认配置：

```text
director_backend = gemma4
director_model = auto
director_mmproj = auto
cache_gemma_preproduction = false
gemma4_mtp = true
```

如果MTP不稳定或需要基准对比：

```text
gemma4_mtp = false
```

`cache_gemma_preproduction=true` 会把Gemma的静态预制作上下文保存到系统RAM。它可能占用数GiB内存，但不占用显存。Qwen不支持此选项。

## 5. 12GB显存推荐设置

建议先从以下配置开始：

```text
video_continuation = 5
cache_gemma_preproduction = false
gemma4_mtp = false       # Qwen必须关闭
tiny_vae = none
```

`chunk_frames` 需要根据分辨率和当前H3模型实测。它表示一次H3调用实际采样的最大帧数：

- 数值越小，显存越低，但导演交接次数越多；
- 数值越大，连续性通常更好，但显存峰值更高；
- H3会把帧数对齐到自身的 `5 + 17k` 时间网格。

发生显存不足时，按以下顺序处理：

1. 减小 `chunk_frames`；
2. 保持 `video_continuation=5`；
3. 使用 `tiny_vae=none`；
4. 关闭 Gemma MTP；
5. 关闭 Gemma 预制作KV缓存不会减少显存，但会减少系统RAM占用。

不要同时运行其他占用GPU的程序。

## 6. 中文提示词和对白

Qwen3.5可以直接理解中文原始提示词。导演输出遵循以下规则：

- H3视觉、环境、人物动作和镜头运动使用英文；
- 原始对白、歌词和画面文字保留原语言；
- `<d>...</d>` 中的原始对白不翻译、不改写；
- `<Subject N>`、`<Picture N>`、`<Video N>` 和 `<Audio N>` 引用保持不变；
- 真实切镜使用 H3 所需的 `[Shot N] At MM:SS.mmm,` 格式。

示例：

```text
[Shot 1]
一名女子站在雨夜的霓虹街道上，她回头看向镜头并说：<d>[Chinese] 你终于来了。</d>
```

导演交给H3的描述应类似：

```text
A woman stands on a neon-lit street at night as rain reflects the red and blue signs across the wet pavement. In a continuous movement, the camera slowly tracks toward her as she turns to face the lens. She says: <d>[Chinese] 你终于来了。</d>
```

## 7. 长视频续接原理

每个分块按以下顺序执行：

1. 卸载 H3、CLIP/Qwen conditioning模型和视频VAE；
2. 启动本地导演独立进程；
3. 首块先规划完整镜头时间表；
4. 后续块解码上一块的抽样画面供导演观察；
5. 导演输出当前块的 H3 `detailed_description`；
6. 导演进程退出，释放 llama.cpp CUDA资源；
7. 重新加载 H3 并完成当前块全部采样步骤；
8. 将上一块末尾的同步Video/Audio续接条件传给下一块。

用于导演观察的画面会转到CPU后再启动导演，避免VAE输出继续占用GPU。

## 8. 输出

`HR Endless Sampler` 输出：

| 输出 | 内容 |
| --- | --- |
| `output` | 完成的长视频/音频latent |
| `denoised_output` | 完整denoised latent |
| `chunk_prompts` | 每块最终发送给H3的提示词及范围 |
| `timeline` | 分块、镜头、提示词和耗时元数据 |

可将 `timeline` 和解码后的图像连接到：

- `HR Endless Sampler Preview`
- `HR Endless Sampler Save Video`
- `HR Endless Sampler Load Video`

## 9. 调试和断点重跑

### `debug`

输出更详细的提示词、阶段和显存记录。

### `debug_stop_chunk`

在指定的1-based分块后停止：

```text
0 = 完整生成
1 = 只生成第1块
2 = 生成到第2块
```

### `debug_start_chunk`

从指定分块重新运行，并复用上一轮保存的前置物理结果：

```text
0 = 正常运行并在下一轮清理旧缓存
2 = 从第2块开始重跑
```

重放缓存会记录导演backend、解析后的模型文件身份、分块布局和提示词来源。切换Gemma/Qwen、替换模型文件或修改关键布局后，不会把旧导演计划静默用于新的A/B测试。

## 10. 日志位置

最近一次运行会写入：

```text
${TMPDIR}/comfyui-hr-endless-sampler/last_gemma_chunk_prompts.txt
${TMPDIR}/comfyui-hr-endless-sampler/last_gemma_images/
```

为兼容旧工具和工作流，文件名仍保留 `gemma`，但内容头部会记录实际使用的 Gemma 4 或 Qwen3.5。

文本日志包括：

- 完整预制作时间计划；
- 每块导演请求；
- 原始JSON响应；
- 修正请求和验证警告；
- 最终发送给H3的提示词。

## 11. 统一测试流程

首次验证建议使用短视频和固定seed。

### Qwen测试

```text
director_backend = qwen3.5
director_model = auto
director_mmproj = auto
cache_gemma_preproduction = false
gemma4_mtp = false
video_continuation = 5
debug = true
debug_stop_chunk = 2
```

检查：

1. 下拉框是否匹配同一目录中的model和mmproj；
2. Qwen是否能完成完整镜头计划；
3. 第2块是否能观察第1块图像；
4. H3提示词的视觉/镜头描述是否为英文；
5. 中文对白是否原样保留；
6. Qwen退出后H3是否正常重新加载；
7. 第2块开始时显存是否回到可运行基线。

### Gemma回归测试

保持完全相同的seed、sampler、sigmas、prompt和分块设置，只修改：

```text
director_backend = gemma4
director_model = auto
director_mmproj = auto
cache_gemma_preproduction = false
gemma4_mtp = false
```

确认旧Gemma流程、提示词、分块结果和timeline正常。随后再单独测试：

```text
gemma4_mtp = true
```

不要同时改变多个参数，否则无法判断差异来源。

## 12. 常见错误

### `Qwen3.5 requires a local GGUF model and mmproj`

没有找到同目录的Qwen模型和projector。明确选择两个文件，或把它们放到同一 `qwen3.5-*` 目录。

### `Qwen3.5 does not support gemma4_mtp`

将：

```text
gemma4_mtp = false
```

### `Qwen3.5 does not support the Gemma preproduction KV cache`

将：

```text
cache_gemma_preproduction = false
```

### `Invalid local director model`

模型必须：

- 是下拉框提供的注册目录内相对路径；
- 位于 `models/LLM/GGUF` 或 `models/llama_cpp`；
- 使用 `.gguf` 扩展名；
- 真实存在于本机。

### `llama-cpp-python`版本错误

确认ComfyUI实际使用的Python中安装的是：

```text
llama-cpp-python==0.3.35
```

不要只检查系统Python。

### 模型加载时显存不足

确认导演开始前日志显示H3、CLIP和VAE已卸载；关闭其他GPU程序，并减小H3的 `chunk_frames`。导演本身运行在独立进程，正常退出后不应继续占用显存。

## 13. 当前限制

- 当前采样backend仅支持MiniMax H3；
- Qwen不支持MTP和跨执行KV缓存；
- Qwen context固定为4096，长提示词和大量镜头可能需要后续实测调整；
- GGUF和mmproj能否成功加载仍取决于本机`llama-cpp-python==0.3.35`对具体Qwen3.5转换格式的支持；
- 自动配对要求model和mmproj位于同一目录；存在多套模型时建议显式选择；
- 12GB显存可用的最大分块长度取决于分辨率、H3量化、attention backend和系统中其他GPU占用。

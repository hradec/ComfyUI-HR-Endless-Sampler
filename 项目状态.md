# HR Endless Sampler 多导演功能项目状态

更新时间：2026-08-30 12:39（Asia/Singapore）

## 1. 项目与版本状态

```text
项目：ComfyUI-MiniMax-H3-Sampler-Unlimited
本地分支：mickeylan
当前提交：19b4749 Merge upstream main into mickeylan
GitHub：github.com/mickeylan/ComfyUI-MiniMax-H3-Sampler-Unlimited
远程分支：mickeylan
```

`19b4749` 已推送到 GitHub 的 `mickeylan` 分支。该提交合并了最新 upstream `main`，并保留此前提交 `2871dbd qwen support` 中的本地 Gemma 4 / Qwen3.5 多导演功能。

当前工作树在编写本文档前是干净的。

## 2. 总体目标

为 `HR Endless Sampler` 提供用户可选的本地多模态导演，同时满足：

- 默认继续使用 Gemma 4；
- 新增 Qwen3.5-9B GGUF + mmproj；
- 用户明确选择 backend、model 和 mmproj；
- 中文输入可由 Qwen理解；
- 交给 MiniMax H3 的视觉、动作和镜头描述优先使用英文；
- 原始对白、歌词和可见文字保留原语言；
- 12GB显存环境中，导演与 H3 严格串行；
- Qwen只使用本地文件，不静默联网下载；
- 保留旧Gemma工作流、timeline和replay兼容性。

## 3. 已完成工作

### 3.1 多导演节点接口

`HREndlessSampler` 已新增以下输入：

```text
director_backend = gemma4 | qwen3.5
director_model
director_mmproj
```

默认后端仍是：

```text
director_backend = gemma4
```

新输入追加在原有输入之后，尽量保持旧工作流和旧位置参数调用兼容。

### 3.2 本地模型发现与路径安全

已新增：

```text
director_backend.py
```

只递归发现以下目录中的 `.gguf` 文件：

```text
models/llama_cpp/
models/LLM/GGUF/
```

已实现：

- model和mmproj分别过滤；
- 拒绝URL；
- 拒绝绝对路径；
- 拒绝包含 `..` 的路径；
- 拒绝模型根目录之外的文件；
- 拒绝非 `.gguf` 文件；
- 拒绝把mmproj当主模型；
- 拒绝把普通模型当mmproj；
- Qwen `auto` 只配对同一目录中的model和mmproj；
- 显式选择Qwen model且mmproj为 `auto` 时，只在该model所在目录寻找projector。

插件不会移动或复制用户模型。

### 3.3 Qwen3.5导演适配器

已新增：

```text
qwen35.py
Qwen35ContinuityDirector
```

Qwen配置为：

```text
director_backend = qwen3.5
director_n_ctx = 4096
director_n_batch = 256
gemma4_mtp = false
```

Qwen复用现有经过验证的：

- 镜头时间规划请求；
- 分块观察请求；
- JSON结果结构；
- 提示词校验；
- timeline数据结构；
- disposable worker协议。

Qwen使用普通 `MTMDChatHandler`，不会使用Gemma专用handler。

### 3.4 Gemma 4兼容性

Gemma仍保留：

- 默认自动模型路径；
- 原提示模板；
- 专用 `Gemma4MTMDChatHandler`；
- MTP assistant；
- 预制作KV缓存；
- operation-local non-MTP retry；
- disposable worker；
- 原有timeline和replay字段。

Gemma还支持用户显式选择本地model和mmproj。

为防止错误搭配，显式本地Gemma只有在路径与官方默认model/projector完全一致时才允许启用 `gemma4_mtp`。

### 3.5 不支持能力的明确错误

Qwen模式下以下设置会直接报错，而不是静默忽略：

```text
cache_gemma_preproduction = true
gemma4_mtp = true
```

正确的Qwen初始配置是：

```text
director_backend = qwen3.5
director_model = auto
director_mmproj = auto
cache_gemma_preproduction = false
gemma4_mtp = false
```

### 3.6 中文输入与H3提示契约

共享导演提示契约要求：

```text
Use English except for original dialogue, lyrics, and visible text.
```

预期行为：

- Qwen理解中文剧情和导演要求；
- 视觉、人物动作、环境和镜头运动用英文表达；
- 中文对白保持中文；
- 原始歌词和画面文字保持原语言；
- H3的Subject/Picture/Video/Audio引用和Shot时间码保持原格式。

### 3.7 显存串行与worker释放

当前执行顺序已经保持：

1. 卸载H3 model；
2. 卸载CLIP/Qwen conditioning model；
3. 必要时卸载VAE；
4. 清理ComfyUI模型缓存；
5. 启动导演独立进程；
6. 等待导演进程完成并退出；
7. 释放llama.cpp CUDA上下文；
8. 再重新加载H3继续采样。

上一块供导演观察的VAE解码帧会复制到CPU，再启动导演worker，避免该观察张量继续占用显存。

### 3.8 Replay隔离

Replay fingerprint现在包含：

- director backend；
- 解析后的model文件路径；
- model文件大小和修改时间；
- 解析后的mmproj文件路径；
- mmproj文件大小和修改时间；
- continuation分辨率；
- 原有latent、chunk和continuation布局。

每个已完成chunk还记录生成它时的原始prompt哈希。切换Gemma/Qwen、替换模型文件或修改prompt后，不会把旧导演文本静默当作当前结果使用。

历史 `gemma_*` 字段和 `last_gemma_*` 文件名继续保留，以兼容已有timeline、replay和外部工具。

### 3.9 Upstream合并

已处理最新upstream `main` 与多导演代码的冲突，并同时保留：

- `source_images` Ref2VA Autogrow；
- `video_continuation_res`；
- upstream默认 `video_continuation=22`；
- PyTorch显存比例限制；
- 自动中断恢复；
- persistent character state；
- Gemma响应修复；
- 32K Q8 KV配置；
- 最新MTP host checkpoint实现；
- upstream预览、工作流和测试更新；
- Gemma/Qwen多导演选择。

冲突已全部消除，merge commit为：

```text
19b4749 Merge upstream main into mickeylan
```

### 3.10 文档

已完成：

```text
README.md
memory.md
dependency.md
使用手册.md
```

`使用手册.md` 包括安装、模型目录、节点连接、Gemma/Qwen配置、12GB建议、中文提示规则、日志、断点重跑、统一测试流程和常见错误。

## 4. 已完成验证

### 4.1 静态验证

已通过：

```text
python -m compileall -q custom_nodes/ComfyUI-MiniMax-H3-Sampler-Unlimited
git diff --check
git diff --cached --check
```

冲突合并后确认：

```text
无 <<<<<<< / ======= / >>>>>>> 冲突标记
无 unmerged 文件
```

### 4.2 导演后端单元测试

以下命令通过：

```text
python custom_nodes/ComfyUI-MiniMax-H3-Sampler-Unlimited/tests/test_director_backend.py
```

结果：

```text
Ran 7 tests
OK
```

覆盖：

- Qwen自动发现model/mmproj；
- 不跨目录错误配对；
- 显式model配对同目录projector；
- 拒绝模型根目录外路径；
- 拒绝绝对路径；
- 拒绝model/mmproj角色错配；
- Qwen请求使用4096 context、256 batch且关闭MTP。

### 4.3 推送验证

GitHub远程 `mickeylan` 已确认指向：

```text
19b47499c66bb4c319e3752a03f3c0be8d41a411
```

## 5. 尚未完成或尚未验证

### 5.1 Qwen真实模型加载

尚未用ComfyUI实际运行Python环境确认：

- `llama-cpp-python==0.3.35` 能否识别当前Qwen3.5 GGUF架构；
- 当前mmproj能否由 `MTMDChatHandler` 正确加载；
- 图像输入能否正确进入Qwen；
- CUDA layers是否按预期offload；
- Qwen chat template是否能稳定生成预期JSON；
- 4096 context是否足够覆盖真实长剧情和镜头规划。

因此当前状态是“代码支持已完成”，不是“真实Qwen GPU推理已验证”。

### 5.2 Qwen导演质量

尚未真实验证：

- 中文剧情理解；
- 英文视觉/动作/镜头输出；
- 中文对白原样保留；
- 严格JSON成功率；
- Shot时间码正确率；
- 多chunk身份、动作、场景和色彩连续性；
- `abliterated` Qwen与官方instruct Q4的质量差异。

### 5.3 12GB显存实测

尚未完成12GB GPU上的端到端测试：

- Qwen加载峰值；
- Qwen退出后的显存释放；
- 第一个H3 chunk；
- Qwen观察第一块并导演第二块；
- 第二个H3 chunk重新加载；
- 连续多块后显存是否增长；
- 1080p工作负载是否稳定；
- director、VAE和H3是否在真实运行中从不同时驻留GPU。

### 5.4 Gemma完整回归

尚未在真实ComfyUI运行环境完成：

- 默认Gemma自动模型；
- 显式本地Gemma model/mmproj；
- `gemma4_mtp=false`；
- `gemma4_mtp=true`；
- `cache_gemma_preproduction=true`；
- upstream最新32K Q8 KV和MTP host checkpoint路径；
- 旧workflow加载后的实际widget值和输出。

### 5.5 完整测试套件

以下测试在当前工具Python中仍被环境依赖阻断：

```text
python tests/test_chunk_director_helpers.py
python tests/test_gemma4_capture.py
```

阻断错误：

```text
ModuleNotFoundError: No module named 'comfy_aimdo'
```

这不是测试断言失败，但意味着完整插件测试尚未在ComfyUI实际Python环境中重新运行。

普通工具Python此前还缺少：

```text
ModuleNotFoundError: No module named 'llama_cpp'
```

必须使用ComfyUI实际启动所用Python完成最终测试。

### 5.6 用户界面细节

尚未在浏览器中确认：

- 三个director下拉框是否正常显示所有本地GGUF；
- 大量模型文件时下拉体验是否可接受；
- 切换backend时不支持选项的提示是否足够清楚；
- upstream前端对隐藏widget和旧工作流位置值的处理是否完全符合预期；
- timeline、preview和日志中是否仍有不必要的“Gemma”硬编码文本。

保留 `gemma_*` 数据字段和日志文件名是有意兼容；用户可见文本仍需通过真实UI检查区分“兼容字段”和“错误标签”。

### 5.7 文档与发布

`使用手册.md` 已完成并已提交，但尚未确认GitHub页面对中文文件名和目录链接的展示体验。README虽已加入Qwen说明，仍可以在真实测试成功后补充：

- 已验证的Qwen模型精确文件名；
- 已验证的mmproj精确文件名；
- llama.cpp加载日志摘要；
- 12GB实测参数；
- 已知最大稳定分块和分辨率；
- Gemma/Qwen画质对比结果。

## 6. 建议的下一步顺序

### 第一阶段：确认实际Python环境

在ComfyUI实际使用的Python中运行：

```bash
python -c "import llama_cpp; print(llama_cpp.__version__)"
python -c "import comfy_aimdo; print(comfy_aimdo.__file__)"
```

预期：

```text
llama_cpp.__version__ == 0.3.35
comfy_aimdo 可以导入
```

### 第二阶段：运行完整非GPU测试

```bash
python -m compileall -q custom_nodes/ComfyUI-MiniMax-H3-Sampler-Unlimited
python -m pytest custom_nodes/ComfyUI-MiniMax-H3-Sampler-Unlimited/tests -q
```

真实GPU live测试应保持默认跳过，除非明确设置其环境变量。

### 第三阶段：Qwen最小加载测试

只验证：

- model架构识别；
- mmproj加载；
- 一张图输入；
- 一次短JSON响应；
- worker正常退出；
- GPU显存回落。

不要直接从20秒1080p长视频开始。

### 第四阶段：两块H3串行测试

推荐初始设置：

```text
director_backend = qwen3.5
director_model = auto
director_mmproj = auto
cache_gemma_preproduction = false
gemma4_mtp = false
video_continuation = 5
tiny_vae = none
debug = true
debug_stop_chunk = 2
```

检查第一块、导演观察、第二块和显存释放。

### 第五阶段：Gemma A/B回归

保持相同seed、sampler、sigmas、prompt、分辨率和chunk设置，只切换：

```text
director_backend = gemma4
director_model = auto
director_mmproj = auto
```

先测试：

```text
cache_gemma_preproduction = false
gemma4_mtp = false
```

随后分别启用KV缓存和MTP，不要一次改变多个变量。

### 第六阶段：长视频与发布结论

短测试全部通过后，再测试：

- 1080p；
- 12GB显存；
- 20秒或更长；
- Ref2VA多图；
- 中英文混合提示；
- 中文对白；
- replay恢复；
- Gemma/Qwen切换后的缓存隔离。

完成后，把真实稳定参数和模型兼容结论补充到README和使用手册。

## 7. 当前结论

当前多导演功能已经完成代码实现、冲突合并、静态验证、路径安全测试、基础适配器测试、文档和GitHub推送。

仍不能宣称Qwen3.5导演已在真实ComfyUI/GPU环境中完整可用。发布前最关键的剩余工作是：

1. 在ComfyUI实际Python中运行完整测试；
2. 验证Qwen3.5 GGUF + mmproj真实加载；
3. 验证中文输入、英文H3镜头描述和中文对白契约；
4. 验证12GB显存下导演与H3严格串行释放；
5. 完成Gemma旧工作流和MTP/KV回归；
6. 完成两块、短视频和长视频GPU测试。

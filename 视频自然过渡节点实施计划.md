# HR MiniMax H3 视频自然过渡节点实施计划

> 文档日期：2026-09-18  
> 项目：ComfyUI-MiniMax-H3-Sampler-Unlimited  
> 状态：设计阶段

## 1. 目标

为两段独立视频 A、B 生成自然的中间桥接片段，避免直接通过 FFmpeg 拼接产生明显的动作、人物、机位、光照或场景跳变。

核心思路：

1. 提取视频 A 的最后 22 帧；
2. 提取视频 B 的最前 22 帧；
3. 使用本地多模态 LLM 分析两侧的人物、动作、构图、镜头和场景状态；
4. 使用人物参考图片锁定脸部、发型、体型及固定身份特征；
5. 由 LLM 设计从 A 末尾到 B 开头的过渡策略，并生成 MiniMax H3 提示词；
6. 使用 MiniMax H3 生成新的桥接视频；
7. 自动搜索低差异接缝并组装 A、桥接段和 B；
8. 对音频独立进行延续、生成或交叉淡化。

最终结构：

```text
视频 A 主体 + H3 生成的桥接段 + 视频 B 主体
```

## 2. 设计原则

### 2.1 三类条件职责分离

| 条件 | 主要职责 |
| --- | --- |
| 人物参考图片 | 确定“这个人是谁”，锁定长相、发型、体型和固定特征 |
| A 最后 22 帧 | 确定过渡从什么动作、姿态、构图和镜头状态开始 |
| B 最前 22 帧 | 确定过渡最终需要到达的动作、姿态、构图和场景状态 |

人物图片不负责时间变化，A/B 边界视频不应取代身份参考图。

### 2.2 LLM 只负责规划

LLM 不直接生成或拼接视频，只输出结构化分析和 H3 提示词，包括：

-人物身份与特征；
- A 末尾状态；
- B 开头状态；
-动作和空间连续性约束；
-镜头及光照过渡；
-适合采用的转场方式；
- MiniMax H3 六字段提示词。

### 2.3 H3 负责生成桥接内容

H3 的条件优先级建议为：

```text
身份：人物参考图片
起点：A 尾部视频
终点：B 头部视频
中间变化：LLM 过渡计划
```

### 2.4 FFmpeg 只负责最终编码

FFmpeg 不承担语义过渡生成，只用于：

-最终视频编码；
-音视频封装；
-必要的音频交叉淡化；
-输出 MP4、MKV、WebM 等容器。

## 3. 推荐节点组

整个功能拆分为四个节点：

```text
HR Video Bridge Extract
        ↓
HR Video Bridge Director
        ↓
HR Video Bridge Sampler
        ↓
HR Video Bridge Assemble
```

拆分节点可避免破坏现有 HR Endless Sampler 的分块、重拍、续写、Timeline 和 replay cache 语义。

---

## 4. HR Video Bridge Extract

### 4.1 职责

提取视频 A、B 的边界帧和音频，统一基础规格，并建立桥接源数据。

### 4.2 输入

```text
video_a: IMAGE
video_b: IMAGE
audio_a: AUDIO（可选）
audio_b: AUDIO（可选）
fps_a: FLOAT
fps_b: FLOAT
analysis_frames: INT，默认 22
fps_policy: reject / resample_b_to_a / resample_a_to_b
target_width: INT（可选）
target_height: INT（可选）
```

建议直接连接现有 `HR Endless Sampler Load Video` 的 `IMAGE`、`AUDIO`、`fps` 和 `frame_count` 输出，不在本节点中重复读取视频文件。

### 4.3 输出

```text
bridge_source: HR_VIDEO_BRIDGE_SOURCE
a_tail_frames: IMAGE
b_head_frames: IMAGE
a_tail_audio: AUDIO（可选）
b_head_audio: AUDIO（可选）
diagnostic: STRING
```

### 4.4 行为

-提取 A 最后 22 帧；
-提取 B 最前 22 帧；
-提取对应时间范围的音频；
-检查帧率、尺寸和通道；
-按显式策略统一帧率；
-保留完整 A/B 视频，供最终组装；
-不在提取阶段裁掉源视频内容。

### 4.5 校验

- A、B 必须至少各有 22 帧；
-默认拒绝静默改变 FPS；
- A/B 输出尺寸必须能对齐 H3 canvas；
- H3 合法时长必须遵循 `5 + 17N` 帧网格；
- 22 帧对应 7 个 H3 视频 latent token。

---

## 5. HR Video Bridge Director

### 5.1 职责

使用本地多模态 LLM 分析人物身份、两侧边界状态和可行转场策略，并生成结构化桥接计划及 H3 提示词。

### 5.2 输入

```text
bridge_source: HR_VIDEO_BRIDGE_SOURCE
director_config: HR_QWEN38_DIRECTOR_CONFIG
reference_set: HR_MINIMAX_H3_REFERENCE_SET
transition_frames: 22 / 39 / 56 / 73
transition_strategy: auto / continuous_action / foreground_occlusion /
                     whip_pan / camera_follow / light_transition / match_cut
identity_policy: strict / balanced / follow_video
clothing_policy: follow_video / lock_a / arrive_at_b / follow_reference
prompt_lang: zh / en
user_instruction: STRING（可选）
```

默认建议：

```text
transition_frames = 39
transition_strategy = auto
identity_policy = strict
clothing_policy = follow_video
```

### 5.3 固定素材顺序

```text
<Picture 1...N>：人物身份参考图片
<Video 1>：A 最后 22 帧
<Video 2>：B 最前 22 帧
```

必须保留稳定顺序和标签映射，不能依赖 LLM 自行猜测图片与人物关系。

### 5.4 两阶段分析

#### 阶段一：身份与状态分析

```json
{
  "subjects": [
    {
      "subject_id": "Subject 1",
      "identity_features": {
        "face": "",
        "hair": "",
        "body": "",
        "clothing": "",
        "accessories": ""
      },
      "state_at_a_end": {
        "position": "",
        "pose": "",
        "expression": "",
        "motion": ""
      },
      "state_at_b_start": {
        "position": "",
        "pose": "",
        "expression": "",
        "motion": ""
      },
      "identity_risks": []
    }
  ],
  "camera_a": {},
  "camera_b": {},
  "environment_a": {},
  "environment_b": {},
  "motion_a": {},
  "motion_b": {}
}
```

#### 阶段二：过渡设计

```json
{
  "strategy": "foreground_occlusion",
  "bridge_action": "",
  "camera_transition": "",
  "lighting_transition": "",
  "audio_transition": "",
  "continuity_constraints": [],
  "h3_prompt": ""
}
```

### 5.5 输出

```text
bridge_plan: HR_VIDEO_BRIDGE_PLAN
h3_prompt: STRING
analysis_json: STRING
risk_report: STRING
```

### 5.6 LLM 运行时边界

新增独立请求类型：

```text
operation = video_bridge
```

要求：

-不得为了桥接功能修改 Qwen3.5 已有推理代码；
- Qwen3.5 保持冻结并作为既有流程对照；
- Qwen3.6/3.8 使用独立 runtime 和 worker；
-第一版优先开放 Qwen3.6/3.8；
- Qwen3.5 仅在真实完整回归后再开放桥接 operation。

---

## 6. HR Video Bridge Sampler

### 6.1 职责

将人物参考图片、A 尾部、B 头部和桥接提示词转换为 H3 conditioning，生成新的中间桥接片段。

### 6.2 输入

```text
bridge_plan: HR_VIDEO_BRIDGE_PLAN
reference_set: HR_MINIMAX_H3_REFERENCE_SET
model
clip
vae
audio_vae
guider
sampler
sigmas
noise
width
height
seed
```

### 6.3 条件映射

#### 人物参考图片

作为 H3 image references：

```python
{
    "kind": "image",
    "latent_h": 0,
    "latent_w": 0,
    "latent": None
}
```

负责稳定人物脸部、发型、体型和固定饰品。

#### A 最后 22 帧

作为桥接开始状态：

```text
<Video 1> / <Audio 1>
```

用于约束：

-初始动作；
-初始姿态；
-画面构图；
-镜头运动趋势；
-环境声连续性。

#### B 最前 22 帧

第一阶段作为第二个视频参考：

```text
<Video 2> / <Audio 2>
```

结合提示词明确要求桥接段趋近 B 的初始状态。

### 6.4 双端约束分阶段实施

#### 第一阶段：双视频参考

```text
A 尾部 → Video 1
B 头部 → Video 2
身份图片 → Picture N
```

优点是可以复用现有 Ref2VA 数据结构；缺点是 Video 2 是语义参考，不等于严格末端锚点。

#### 第二阶段：双端 latent anchoring

如果实机测试证明普通 Video 2 不能稳定抵达 B，再研究：

```text
A 尾部 latent → 首端 keyframe
B 头部 latent → 末端 keyframe
```

实施前必须验证：

-当前 ComfyUI H3 是否支持尾端 keyframe；
-多 token 末端视频锚定；
- keyframe 与 refs 同时打包；
-旧版 H3 payload 覆盖兼容；
-音频末端锚定；
-目标帧重复和裁切规则。

### 6.5 输出

```text
bridge_latent: LATENT
bridge_denoised: LATENT
bridge_frames: IMAGE
bridge_audio: AUDIO
bridge_timeline: HRENDLESS_TIMELINE
```

---

## 7. HR Video Bridge Assemble

### 7.1 职责

在 A、桥接段和 B 的重叠区域中选择低差异接缝，裁掉重复锚定内容，完成视频与音频组装。

### 7.2 输入

```text
bridge_source: HR_VIDEO_BRIDGE_SOURCE
bridge_frames: IMAGE
bridge_audio: AUDIO（可选）
assemble_policy: auto_seam / fixed_trim / crossfade / hard_cut_debug
seam_search_frames: INT
audio_policy: crossfade_original / use_bridge_audio /
              preserve_dialogue / mute_bridge
crossfade_seconds: FLOAT
```

### 7.3 视频接缝策略

需要分别搜索：

```text
A → Bridge
Bridge → B
```

第一版不引入新的视觉模型依赖，使用以下指标：

-缩小图像后的 L1/L2 差异；
-平均亮度差；
-颜色统计差异；
-边缘结构差异。

后续可选增加：

-感知特征差异；
-光流方向；
-人物关键点漂移；
-人脸身份相似度。

### 7.4 Assemble policy

| 策略 | 行为 |
| --- | --- |
| `auto_seam` | 在重叠搜索区内选择综合差异最低的切点 |
| `fixed_trim` | 固定裁掉指定数量的 A 尾、Bridge 首尾和 B 头帧 |
| `crossfade` | 在模型过渡基础上增加轻微视觉交叉淡化 |
| `hard_cut_debug` | 不做融合，用于判断模型是否真正生成了连续桥接 |

### 7.5 音频策略

| 策略 | 行为 |
| --- | --- |
| `crossfade_original` | A 和 B 原始音频围绕桥接段交叉淡化 |
| `use_bridge_audio` | 使用 H3 生成的桥接音频并与两端交叉淡化 |
| `preserve_dialogue` | 优先保留原对白，只生成或融合环境声 |
| `mute_bridge` | 桥接段静音，仅用于视觉测试 |

默认建议：

```text
audio_policy = use_bridge_audio
crossfade_seconds = 0.25
```

### 7.6 输出

```text
frames: IMAGE
audio: AUDIO
timeline: HRENDLESS_TIMELINE
seam_report: STRING
```

Timeline 示例：

```json
{
  "segments": [
    {"type": "source_a", "start": 0, "end": 200},
    {"type": "generated_bridge", "start": 201, "end": 239},
    {"type": "source_b", "start": 240, "end": 500}
  ],
  "seams": [
    {"frame": 201, "score": 0.08},
    {"frame": 240, "score": 0.06}
  ]
}
```

---

## 8. Typed 数据结构

建议新增：

```text
HR_VIDEO_BRIDGE_SOURCE
HR_VIDEO_BRIDGE_PLAN
HR_VIDEO_BRIDGE_RESULT
```

### 8.1 HR_VIDEO_BRIDGE_SOURCE

```python
{
    "version": 1,
    "fps": 24.0,
    "a_frames": "tensor",
    "b_frames": "tensor",
    "a_tail": "tensor",
    "b_head": "tensor",
    "a_audio": "optional audio",
    "b_audio": "optional audio",
    "analysis_frames": 22,
    "source_a_frame_count": 0,
    "source_b_frame_count": 0,
}
```

### 8.2 HR_VIDEO_BRIDGE_PLAN

```python
{
    "version": 1,
    "transition_frames": 39,
    "subjects": [],
    "strategy": "",
    "constraints": [],
    "h3_prompt": "",
    "reference_labels": {
        "pictures": [],
        "video_a": "<Video 1>",
        "video_b": "<Video 2>"
    }
}
```

### 8.3 HR_VIDEO_BRIDGE_RESULT

```python
{
    "version": 1,
    "bridge_frames": "tensor",
    "bridge_audio": "optional audio",
    "left_seam": {},
    "right_seam": {},
    "timeline": {}
}
```

大 tensor 只保存在 typed payload 中，不写入 JSON。调试 JSON 只记录 shape、FPS、范围、标签和摘要。

---

## 9. 人物身份约束

### 9.1 推荐参考图片

每个人物建议提供：

-一张清晰正脸；
-一张左侧或右侧脸；
-一张半身或全身图；
-必要时增加服装和固定配饰细节图。

### 9.2 多人物映射

必须显式建立：

```text
Subject 1 → Picture 1, Picture 2, Picture 3
Subject 2 → Picture 4, Picture 5
```

不得让 LLM 根据图片顺序自行猜测人物对应关系。

### 9.3 冲突处理

当参考图片与视频状态冲突时，默认优先级为：

```text
脸部和身份：参考图片优先
服装：A/B 视频优先
A 端姿态：A 尾部优先
B 端姿态：B 头部优先
中间动作：桥接计划优先
```

---

## 10. 推荐转场策略

### 10.1 连续动作

适用：人物、场景和机位相近。

示例：

-继续转身；
-完成抬手；
-继续行走；
-视线自然移动到 B 的方向。

### 10.2 前景遮挡

适用：场景、姿态或机位差异较大。

示例：

-人物经过镜头；
-柱子、门框或车辆遮挡；
-镜头穿过暗处；
-主体贴近镜头形成短暂全画面遮挡。

### 10.3 Whip pan

适用：机位变化明显但运动方向可以匹配。

通过快速摇镜、运动模糊和相同方向的相机速度隐藏场景切换。

### 10.4 光照转场

适用：环境结构接近但亮度、色温不同。

通过曝光变化、闪光、进入阴影或穿过强光完成过渡。

### 10.5 Match cut

适用：A、B 中存在形状、姿势或构图相似元素。

LLM 应明确指出匹配对象和切换时机，不应只输出泛化的“自然过渡”。

---

## 11. 第一版实施范围

第一版完成：

1. 提取 A 尾部和 B 头部各 22 帧；
2. 接收人物参考图片；
3. 使用 Qwen3.6/3.8 分析双边界视频；
4. 输出结构化桥接计划；
5. 编译 H3 六字段提示词；
6. 将 A/B 作为两个视频 references；
7. 生成 39 帧桥接段；
8. 自动搜索左右接缝；
9. 对音频执行交叉淡化；
10. 输出 Timeline 和接缝报告。

第一版暂不完成：

-大型浏览器时间线编辑器；
-全局素材库；
-任意数量视频的批量串联；
-人脸识别或人脸替换模型；
-额外光流框架；
-严格 B 端 latent 硬锚定；
-修改 Qwen3.5；
-将桥接伪装为 continuation checkpoint 或 retake chunk。

---

## 12. 测试计划

### 12.1 单元测试

- A/B 恰好 22 帧；
- A/B 少于 22 帧时明确拒绝；
-长视频正确截取尾部和头部；
-不同 FPS 的三种策略；
-不同分辨率的统一处理；
-22 帧正确转换为 7 个 H3 latent token；
-39、56、73 帧均符合 H3 时间网格；
-参考图片顺序与 Subject 映射保持稳定；
- A 固定为 Video 1，B 固定为 Video 2；
- typed payload 不被原地修改；
- assembly 不重复 A 尾或 B 头；
-视频帧数与音频时长一致；
- Timeline 范围与实际输出一致；
- Qwen3.5 文件和 worker 路径没有变化。

### 12.2 实机测试矩阵

| 场景 | 验证目标 |
| --- | --- |
| 同人物、同场景、轻微姿势差 | 基础动作连续性 |
| 同人物、不同机位 | 相机运动过渡 |
| 同人物、不同场景 | 遮挡或运镜转场 |
| 两个人物同框 | 身份不互换 |
| 快速运动 | 动作方向和速度连续 |
| 正在说话 | 对白和口型风险 |
| 光照差异明显 | 曝光与色温过渡 |
| 服装存在细节 | 服装纹理稳定性 |
| 12GB VRAM | 峰值显存与可执行性 |

### 12.3 验收标准

-桥接段人物没有明显换脸；
-人物数量不发生异常变化；
- A 到 Bridge 没有明显姿势跳变；
- Bridge 能进入接近 B 开头的状态；
-没有重复播放完整的 A 尾 22 帧或 B 头 22 帧；
-左右接缝没有明显亮度闪变；
-音频没有爆音、突停或明显时间漂移；
-输出帧数与 Timeline 完全一致；
-默认 39 帧桥接可在目标 12GB VRAM 环境运行。

---

## 13. 风险与限制

### 13.1 Video 2 不是严格末端锚点

普通 Ref2VA 视频 reference 只能提供语义和运动参考，不能保证桥接段最后一帧与 B 第一帧完全一致。是否需要双端 latent anchoring，必须由实机结果决定。

### 13.2 场景差异过大

若 A 与 B 在人物、空间、机位或时间上完全矛盾，短桥接段无法真实地逐帧变形。Director 应主动选择遮挡、摇镜、强光或 match cut 等电影转场。

### 13.3 身份图与视频冲突

人物参考图和视频中的年龄、服装或发型差异过大时，模型可能产生身份漂移。需要通过 `identity_policy` 和 `clothing_policy` 明确优先级。

### 13.4 音频不能只做视觉式处理

对白、音乐和环境声应分别处理。简单线性交叉淡化不能解决节拍、调性或句子中断问题。

### 13.5 显存压力

双视频 reference、人物图片和较长桥接段会增加 H3 packed rows 和 Qwen 视觉 token。需要记录：

- Qwen 编码峰值显存；
- H3 attention rows；
-桥接段采样峰值显存；
- VAE 编解码峰值；
-不同 reference resolution 的质量/显存差异。

---

## 14. 实施顺序

### 阶段一：数据契约

1. 定义三个 typed payload；
2. 实现 A/B 边界提取；
3. 实现 FPS、帧数、尺寸及音频校验；
4. 增加对应单元测试。

### 阶段二：Director

1. 新增 `video_bridge` operation；
2. 定义严格 JSON schema；
3. 固定人物图片、Video 1 和 Video 2 标签；
4. 编译 H3 六字段提示词；
5. 保持 Qwen3.5 文件不变。

### 阶段三：H3 桥接生成

1. 复用现有 Reference Set 的图片 conditioning；
2. 编码 A/B 视频 reference；
3. 生成 39 帧桥接 latent；
4. 验证音视频时长和 packed layout；
5. 测量 12GB VRAM 峰值。

### 阶段四：组装和接缝

1. 实现固定裁切调试模式；
2. 实现基础 seam score；
3. 实现自动左右接缝；
4. 实现音频交叉淡化；
5. 生成 Timeline 和 seam report。

### 阶段五：双端硬锚定研究

仅在 Video 2 语义约束不足时开始：

1. 验证末端 keyframe API；
2. 验证多 token 尾端锚定；
3. 验证 keyframes 与 refs 共存；
4. 验证音频尾端 anchor；
5. 对比普通双 reference 与双端 latent anchor 的质量、显存和稳定性。

---

## 15. 最终用户工作流示例

```text
Load Video A ─┐
              ├─→ HR Video Bridge Extract
Load Video B ─┘              │
                             ▼
人物参考图片 → HR MiniMax H3 Reference Set
                             │
                             ▼
                 HR Video Bridge Director
                             │
                             ▼
              HR Video Bridge Conditioning
                             │
                             ▼
                  HR Endless Sampler
                             │
                    VAE Decode（桥接帧）
                             │
                             ▼
                 HR Video Bridge Assemble
                             │
                             ▼
              HR Endless Sampler Save Video
```

核心原则：

> 人物参考图片锁定“是谁”，A 尾部锁定“从哪里开始”，B 头部锁定“必须到哪里”，LLM 设计“中间如何自然到达”，H3 生成桥接内容，Assembler 负责找到最自然的实际切点。

## 16. 当前实现状态（2026-09-18）

已实现并注册：

- `HR Video Bridge Extract`：提取 A 尾与 B 头各 22 帧，同步截取音频并处理显式 FPS 策略；
- `HR Video Bridge Director`：仅调用隔离的 Qwen3.6/3.8 `video_bridge` operation，结合人物参考图生成结构化计划和 H3 提示词；
- `HR Video Bridge Conditioning`：人物图和 B 头作为 H3 references，A 尾作为首端 continuation keyframe，输出可直接连接现有 `HR Endless Sampler` 的 positive、latent、external_continuation、reference_set 和 prompt；
- `HR Video Bridge Assemble`：支持自动低差异接缝或硬切调试模式，组装 A、桥接帧、B，生成等时长音频、Timeline 和 seam report；
- Bridge Timeline 的 `segments`、`seams` 和 `audio` 扩展可通过现有 Save/Load sidecar 保留。

第一版明确限制：B 头当前是 H3 Video reference 与提示词终点约束，不是严格的末端 latent 硬锚点。是否增加双端 latent anchoring，应根据真实 GPU 生成结果决定。
